# Backend (Express + WhatsApp Service)
NODE_ENV=production
PORT=5000

# Frontend (Vite) — set to your backend deployment URL
# When empty, the frontend connects to the same origin (for local dev)
VITE_BACKEND_URL=https://your-backend-deployment.com